function mostrarPropiedadesWindow() {
    // Obtener una referencia al objeto Window
    var ventana = window;
  
    // Ejemplo de uso
    console.log("Ejemplo de uso de algunos métodos del objeto Window:");
    console.log("Ancho de la ventana: " + ventana.innerWidth);
    console.log("Alto de la ventana: " + ventana.innerHeight);
    ventana.alert("¡Hola, mundo!");
  }
  
// Llamar a la función
 mostrarPropiedadesWindow();